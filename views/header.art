<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link rel="stylesheet" href="css/index.css">
	<title>{{ title }}</title>
</head>

<body>
<div id="window">
	<div class="window-top">
		<a href="{{url}}" class="logo-img">
			<img src="logos/poyo_globe.png" alt="PoyoWeb" class="logo-img">
		</a>
		<% if (loggedIn) { %>
			<a href="{{url}}/auth/logout" class="logout">Logout</a>
		<% } else { %>
			<a href="{{url}}/auth/login" class="login">Login</a>
		<% } endif %>
		<h1>
			{{ title }} - <a href="{{url}}" class="hidden-link">PoyoWeb!</a>
		</h1>
	</div>
<div class="window-content">