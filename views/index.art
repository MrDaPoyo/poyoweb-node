<%- include("./header") %>
<h1>Welcome to the PoyoWeb!</h1>
<p>PoyoWeb! *<i>silliness</i>*, all the way!</p>
    {{ if user }}
        <p>Hi, <%= user.username %>.</p>
    {{ if user.verified === 1 }}
        <p>Go to the <a href="/dashboard">dashboard</a>.</p>
    {{ else }}
        <p>Verify your email address to access the <b>dashboard</b>.</p>
    {{ /if }}
{{ else }}
    <p>Hi, guest.</p>
{{ /if }}